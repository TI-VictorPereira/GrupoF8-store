# Desenvolvimento. O banco é o Neon — não há Postgres aqui.
# Subir com:  docker compose up --build
#
# Portas na faixa 88xx de propósito: esta máquina já roda outros containers
# (a Hub F8 API na 8000, frontends na 5173/5174 e um Postgres na 5432).

services:
  api:
    build: ./api
    env_file: ./api/.env
    ports:
      - "8801:8000"
    volumes:
      # Código montado para o --reload pegar as alterações. O diretório do
      # pacote instalado é preservado para o -e não ser sobrescrito.
      - ./api/app:/app/app
      - ./api/alembic:/app/alembic
      - ./api/tests:/app/tests
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  web:
    build: ./web
    depends_on:
      - api
    ports:
      - "8800:5175"
    # Só o que é editado a quente é montado. /app/node_modules fica sendo o da
    # imagem, sem volume por cima — assim mexer em package.json e subir com
    # --build basta para as dependências novas aparecerem.
    volumes:
      - ./web/src:/app/src
      - ./web/index.html:/app/index.html
      - ./web/vite.config.ts:/app/vite.config.ts
      - ./web/tsconfig.json:/app/tsconfig.json
    command: npm run dev
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
